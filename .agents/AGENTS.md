# Customization Rules

- **Do Not Write Comments**: When generating or modifying code, do NOT include any comments (either line comments like `// ...` or block comments like `/* ... */` or JSX comments). Keep all code clean and comment-free.
